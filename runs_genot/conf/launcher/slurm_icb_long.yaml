# @package _global_

defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    partition: gpu_p
    qos: gpu_long
    cpus_per_task: 6
    mem_gb: 80
    gres: gpu:1
    timeout_min: 1440
    array_parallelism: 6 # max num of tasks to run in parallel (via job array)
